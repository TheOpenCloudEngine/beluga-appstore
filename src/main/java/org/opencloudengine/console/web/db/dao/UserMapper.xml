<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="org.opencloudengine.bss.sample.dao.UserMapper" >
  <select id="selectAll" resultType="org.opencloudengine.bss.sample.entity.User">
  	select * from user
  </select>

  <select id="select" parameterType="map" resultType="org.opencloudengine.bss.sample.entity.User">
  	select * from user
  	where id = #{id} and `password`=#{password} and tenantId = #{tenantId}
  </select>

    <select id="selectById" parameterType="map" resultType="org.opencloudengine.bss.sample.entity.User">
        select * from user
        where id = #{id} and tenantId = #{tenantId}

    <select id="selectByTenant" parameterType="map" resultType="org.opencloudengine.bss.sample.entity.User">
        select * from user
        where tenantId = #{tenantId}
    </select>

    <insert id="insert"  parameterType="org.opencloudengine.bss.sample.entity.User">
        INSERT INTO user(
            id, name, `password`, tenantId, admin
        ) VALUES(
            #{id}, #{name}, #{password}, #{tenantId}, #{isAdmin}
        )
    </insert>

    <delete id="truncate" >
        truncate table user
    </delete>
</mapper>